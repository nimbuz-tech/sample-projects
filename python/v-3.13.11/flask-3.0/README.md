# Flask Sample Application (Python 3.13.x)

This is a simple Flask web application built using **Flask 3.0.x** and **Python 3.13.x**.  
It renders an HTML template and displays the Python and Flask versions on the homepage.

---

## 🚀 Features
- Flask 3.0
- HTML template rendering
- Displays Python & Flask versions on the homepage
- Easy to run locally

---

## 📁 Project Structure

```
.
├── app.py
├── requirements.txt
└── templates/
    └── index.html
```

---

## 🧩 Run Locally

### 1️⃣ Create and activate virtual environment
```bash
python3 -m venv venv
source venv/bin/activate      # macOS / Linux
venv\Scripts\activate         # Windows
```

### 2️⃣ Install dependencies
```bash
pip install -r requirements.txt
```

### 3️⃣ Start the application
```bash
python app.py
```

Visit: **http://localhost:8000**

---
